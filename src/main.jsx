import React from 'react';
import ReactDOM from 'react-dom/client';
import AppIDA from './AppIDA';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppIDA />
  </React.StrictMode>
);